# 第三方组件与许可义务

> 本文件记录**全部**第三方依赖及其许可。`NOTICE` 是它的一个子集：只列随二进制
> 分发出去的那些。
>
> 项目自身许可是 **Apache-2.0**（见 `LICENSE`）。

## 1. 必须随二进制分发

| 组件 | 版本 | 许可 | 本项目的使用方式 | 义务 |
|---|---|---|---|---|
| **Qt** | 6.8.3 | **LGPL-3.0**（Qt 开源版；另有 GPL-3.0 / 商业双授权） | 动态链接 `Qt6Core`、`Qt6Gui`、`Qt6Widgets` | 见 §1.1 |

### 1.1 Qt 的义务清单

LGPL-3.0 允许把库链接进**非** (L)GPL 的应用（本项目正是这种情形），但要求：

| 义务 | 出处 | 本项目如何满足 |
|---|---|---|
| **动态链接**，使用户能替换库 | LGPL-3.0 §4(a) | 链接 `Qt6Core.dll` / `Qt6Gui.dll` / `Qt6Widgets.dll`，**不静态链接** |
| 随分发提供 LGPL-3.0 全文 | LGPL-3.0 §4(a) | 发行包内附 `LICENSE.LGPLv3`（全文） |
| 声明使用了 Qt、指明版本与许可 | LGPL-3.0 §1、§4 | `NOTICE` 中已写明模块、版本与许可 |
| 允许用户重新链接 | LGPL-3.0 §4 | 动态链接即满足；DLL 可被替换 |
| 若修改了 Qt 本身则需公开修改 | LGPL-3.0 §4 | **未修改** Qt 源码，不适用 |

**为什么 LGPL 全文不进这个仓库**：仓库里没有 Qt 的代码，附一份 LGPL 全文会产生
「这里有 LGPL 代码」的误导。全文属于**发行包**，由打包步骤放入。

### 1.2 刻意不使用的 Qt 模块

| 模块 | 为什么不用 |
|---|---|
| **Qt Charts** | 只有 **GPL-3.0** 或商业授权，**没有 LGPL**。引入它会把整个分发物拉进 GPL-3.0。绘图改用 QCustomPlot（MIT）或 QWT（LGPL） |
| **Qt Quick / QML**（`qtdeclarative`） | 技术决策，见 ADR-0006：本项目的界面是实验仪器，不是触摸应用；QML 会把调试分成两个运行时 |

这两条不是偏好，是**分发约束**：任何一条被无意中违反，Apache-2.0 分发的目标就失效了。
`scripts/check_qt_modules.ps1` 会检查链接进来的 Qt 模块集合。

## 2. 仅构建/测试期使用，不随二进制分发

| 组件 | 版本 | 许可 | 用途 |
|---|---|---|---|
| **Catch2** | v3.5.2 | BSL-1.0（Boost Software License 1.0） | 单元与契约测试框架 |
| **CMake** | ≥ 3.24 | BSD-3-Clause | 构建系统 |
| **Python** | 3.x | PSF-2.0 | 门禁脚本（`scripts/*.py`） |

BSL-1.0 是宽松许可，无分发义务；`external/Catch2` 由
`scripts/fetch_deps.ps1` 拉取且**不入库**，所以它既不在 `NOTICE` 里，也不出现在
任何发行包中。

## 3. 规划中但尚未使用的依赖

| 组件 | 许可 | 状态 |
|---|---|---|
| **Eigen** | MPL-2.0（自 3.1.1 起） | `docs/plan-tree.md` 列为线性代数选择；**当前代码库尚未引入**。层级门禁已限制它只能出现在 `eval/` 与 `kernels/`。引入时须登记到上表 §1.2 |
| **QCustomPlot** | MIT | 计划中的绘图组件（替代 Qt Charts） |
| **QWT** | LGPL-3.0（含例外） | 绘图备选 |
| **RapidCheck** | MIT | 性质测试，见 `standards/test-taxonomy.md` |
| **Google Benchmark** | Apache-2.0 | 性能红线 |

> 这一节存在的理由：**依赖决策是架构决策的一部分**，而且它们的许可后果往往在
> 引入之后才被发现。把「计划用的」与「正在用的」分开列，可以让一次引入变成一次
> 有意识的登记，而不是一次意外。

## 4. 维护规则

1. **引入任何新依赖时，在同一提交里登记到本文件。** 未登记的依赖是一次未记录的决定。
2. 引入**链接进二进制**的依赖时，同时更新 `NOTICE`（Apache-2.0 §4(d) 要求分发物
   携带 NOTICE 中的归属声明）。
3. 引入 copyleft 依赖前先确认它与 Apache-2.0 的兼容方向：Apache-2.0 代码**可以**
   被并入 LGPL-3.0/GPL-3.0 作品，反之不然。Qt 属于这种单向情形，所以可用。
